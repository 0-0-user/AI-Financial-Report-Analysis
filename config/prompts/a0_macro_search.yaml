# ==========================================================
# config/prompts/a0_macro_search.yaml — A0 层：宏观搜索（已弃用）
# ==========================================================
#
# 本文件已弃用，请使用 a0_macro_facts.yaml 代替。
# 保留此文件仅为保持兼容性，避免测试和旧代码报错。
# 新开发请使用：
#   - a0_search_query.yaml（生成搜索词）
#   - a0_macro_facts.yaml（提取宏观事实）
#
name: a0_macro_search
description: "（已弃用）请使用 a0_macro_facts.yaml"

messages:
  - role: system
    content: |
      【此 prompt 已弃用】
      请使用 a0_macro_facts.yaml（提取事实）和 a0_search_query.yaml（生成搜索词）。
